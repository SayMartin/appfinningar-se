---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import { t, type Lang } from '../i18n/ui';
import { captions, fallbackAlt } from '../data/photos';

interface Props {
  lang: Lang;
  /** Show only the first N — used for the selection on the home page. */
  limit?: number;
}
const { lang, limit } = Astro.props;
const tr = t(lang);

// The images are picked up from the folder automatically. Drop a file in and
// it is on the site at the next build — no list to remember to update.
const modules = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/photos/*.{jpg,jpeg,png,webp,avif}',
  { eager: true },
);

const all = Object.entries(modules)
  .map(([path, mod]) => {
    const filename = path.split('/').pop()!;
    return { filename, image: mod.default, alt: captions[filename]?.[lang] ?? fallbackAlt(filename) };
  })
  .sort((a, b) => a.filename.localeCompare(b.filename));

for (const photo of all) {
  if (!captions[photo.filename]) {
    console.warn(`[photos] ${photo.filename} has no alt text — add it in src/data/photos.ts`);
  }
}

const photos = limit ? all.slice(0, limit) : all;
---

{photos.length === 0 ? (
  <p class="todo">{tr('photos.empty')}</p>
) : (
  <ul class="grid">
    {photos.map(({ image, alt }) => (
      <li>
        {/* widths + sizes make phones load a 400px variant instead of the
            original. Astro generates the variants at build time. */}
        <Image
          src={image}
          alt={alt}
          widths={[400, 800, 1200]}
          sizes="(max-width: 640px) 100vw, (max-width: 900px) 50vw, 22rem"
          format="webp"
          quality={80}
          loading="lazy"
          decoding="async"
        />
        {alt && <span class="caption">{alt}</span>}
      </li>
    ))}
  </ul>
)}

<style>
  .grid {
    list-style: none; margin: 0; padding: 0;
    display: grid; gap: 1rem;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 15rem), 1fr));
  }
  li { margin: 0; }
  li { position: relative; }
  img {
    width: 100%; height: auto; display: block;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: var(--surface);
    transition: transform .3s ease, border-color .3s ease;
  }
  li:hover img { transform: scale(1.015); border-color: var(--border-hov); }
  .caption {
    display: block; margin-top: .5rem;
    font-size: .86rem; color: var(--muted);
  }
</style>
